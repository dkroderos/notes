---
title: Debian
sidebar_position: 1
---

### Must have packages

```
sudo apt-get update
sudo apt-get install nala git vim htop btop ncdu linux-headers-$(uname -r) gcc make ufw xclip zip unzip
```

### Must have applications

```
sudo apt-get update
sudo apt-get install timeshift gparted vlc obs-studio gdebi
```

### Must do

#### Setup firewall

```
sudo ufw enable
```